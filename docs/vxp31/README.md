# VXP-3.1 — Runtime Visual Acceptance + Brand Asset Closure

Stacked on VXP-3 draft PR #25 (`vxp/vxp-3-pedestrian-pursuit-brand-chrome`).

## Goals
- Prove presentation across the real Godot player journey (`REAL_RUNTIME_CAPTURE`).
- Close digital brand gaps: typography (Barlow Condensed OFL), launcher/splash (original Kinetic Sole).
- Repair visual defects found in authentic captures.
- Keep human / Pixel / merge gates honest (`false`).

## Commands
```bash
make vxp31-structural
make vxp31-capture
make vxp31-gates
make headless-smoke
make stream-c-exhaust
```

## Artifacts
See `artifacts/vxp31/` — manifests, reports, capture PNGs, brand contact sheet.
